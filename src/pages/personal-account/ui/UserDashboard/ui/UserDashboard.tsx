import { FC } from 'react';

import styles from './UserDashboard.module.scss';

const UserDashboard: FC = () => {
  return <div className={styles.container}>UserDashboard</div>;
};

export default UserDashboard;
